import streamlit as st


def app():
    st.title('📘 แนวทางการพัฒนาโมเดล')
    st.markdown('''

## 1. การเตรียมข้อมูล (Data Preparation)

ข้อมูลที่ใช้ในโปรเจคนี้มาจากชุดข้อมูล Dog Breed Identification บน Kaggle ที่ได้ChatGPTแนะนำมา
ซึ่งประกอบด้วยภาพสุนัขหลายสายพันธุ์พร้อมป้ายกำกับ (label) ของสายพันธุ์

ขั้นตอนการเตรียมข้อมูลประกอบด้วย

- ดาวน์โหลดชุดข้อมูล
- แยกข้อมูลภาพตามสายพันธุ์
- แบ่งข้อมูลเป็นชุดฝึก (Training set) และชุดทดสอบ (Validation set)
- ปรับขนาดภาพให้มีขนาดเท่ากันเพื่อใช้ในการฝึกโมเดล

## 2. ทฤษฎีของอัลกอริทึม

Convolutional Neural Network (CNN) เป็นโมเดลในกลุ่ม Neural Network ที่ออกแบบมาสำหรับการประมวลผลข้อมูลภาพโดยเฉพาะ โดยสามารถเรียนรู้ลักษณะสำคัญของภาพ เช่น รูปร่าง สี และลวดลาย ได้โดยอัตโนมัติ

CNN แตกต่างจาก Neural Network ทั่วไปตรงที่สามารถรักษา “โครงสร้างเชิงพื้นที่” ของภาพ (Spatial Structure) ทำให้เหมาะกับงานด้าน Image Classification เช่น การจำแนกสายพันธุ์สุนัข

โมเดลที่ใช้พัฒนาคือ EfficientNetB0 ซึ่งเป็นหนึ่งในสถาปัตยกรรม CNN ที่มีประสิทธิภาพสูง โดยใช้แนวคิดการปรับขนาดโมเดลอย่างสมดุล (Compound Scaling) ทำให้ได้ผลลัพธ์ที่ดีโดยใช้ทรัพยากรน้อยกว่าโมเดลอื่นๆ ในกลุ่มเดียวกัน
EfficientNet เป็นโมเดล CNN ที่ถูกออกแบบให้มีประสิทธิภาพสูง โดยใช้แนวคิด
“Scaling อย่างสมดุล” คือเพิ่มทั้ง
1. ความลึก (Depth)
2. ความกว้าง (Width)
3. ความละเอียดภาพ (Resolution)
4. พร้อมกัน ทำให้ได้ accuracy สูง แต่ใช้ทรัพยากรน้อย
## 3. ขั้นตอนการพัฒนาโมเดล

ขั้นตอนหลักของการพัฒนาโมเดล Machine Learning มีดังนี้

1. การรวบรวมข้อมูล (Data Collection)
2. การเตรียมและจัดรูปแบบข้อมูล (Data Preparation)
3. การสร้างโมเดล (Model Building)
4. การฝึกโมเดล (Model Training)
5. การประเมินผลโมเดล (Model Evaluation)
6. การนำโมเดลไปใช้งานจริง (Deployment)
                
## 4. แหล่งข้อมูลและเครื่องมือที่ใช้

1. ชุดข้อมูล Dog Breed Identification จาก Kaggle
2. ใช้เครื่องมือ Python และไลบรารีต่างๆ เช่น TensorFlow, Keras สำหรับการพัฒนาโมเดล
3. พัฒนาโมเดลด้วยการใช้ Convolutional Neural Networks (CNN) ซึ่งเหมาะสำหรับงานจำแนกประเภทภาพ
4. ใช้เทคนิคการเพิ่มข้อมูล (Data Augmentation) เพื่อเพิ่มความหลากหลายของข้อมูลฝึกและช่วยลดปัญหาการ overfitting
5. ใช้เทคนิคการปรับแต่งโมเดล (Model Tuning) เช่น การปรับค่าพารามิเตอร์ต่างๆ เพื่อเพิ่มประสิทธิภาพของโมเดล
6. โมเดลที่ใช้พัฒนาคือ EfficientNetB0

## 5. วิธีใช้งานโมเดล
1. ผู้ใช้งานสามารถอัปโหลดภาพสุนัขที่ต้องการทำนายสายพันธุ์
2. เมื่อผู้ใช้กดปุ่ม "Predict" 
3. โมเดลจะประมวลผลภาพและแสดงผลลัพธ์การทำนาย 5 อันดับแรกของสายพันธุ์สุนัขที่มีความเป็นไปได้สูงสุด พร้อมกับค่าความเป็นไปได้ในรูปแบบเปอร์เซ็นต์
''')

    if st.button('🚀 ไปหน้า Dog Predictor'):
        st.session_state.page = 'Dog Predictor'
        st.rerun() 


if __name__ == '__main__':
    app()
